#include <stdio.h>
#include <stdlib.h>
#include <stdbool.h>
#include <math.h>
#include <GL/glut.h>
// #include <glut.h>
#include <gl.h>
#include "Vector.h"

// ------------------INTERMEDIATE LEVEL: VECTORS AND DISTANCES --------------------- 
// When program lauched:
//      one red cross in the center of window,
//      one segment between points (100,100) and (300,100) are displayed.
// Each time user presses left mouse click, program calculates distance between
//  center of window(red cross) and mouse click position, and also distance between
//  line segment and mouse click position, then prints these values on the terminal
// ------------------------------------------------------------ --------------------- 
